/**
 * Region-growing districting (scaffold).
 * For v1 we use rectangular only; this can be implemented later for irregular grids.
 */
export function regionGrowDistricting(
  _districtId: Int32Array,
  _activeMask: Uint8Array,
  _width: number,
  _height: number,
  _numDistricts: number
): void {
  throw new Error('Region growing districting not implemented; use rectangular.');
}
